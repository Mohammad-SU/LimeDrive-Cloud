improve file moving functionality with dragging multiple items and adding move btn and modal
in toolbar move button, start the folder list from the parent of the current directory the user is in, use dropbox for inspiration of other move modal features

move to and highlight new folder after user creates it
make tabbing to a file name and pressing enter cause it to open (check folder for this code)
fix file upload selection where user selecting same file again if it was the directly previous selected file not working
add more specific/better icons for files in FileList and UploadInfo
turn beginning of breadcrumb into elipses/dropdown to prevent overflow on long paths
make website title reflect page/folder accurately
change preview toolbar button to "open" if folder is selected, and hide this button if multiple items are selected
change styles for folder/file name spans on hover?
folder uploading
set currentPath to root (LimeDrive/) when user goes to a separate/unrelated page
test if laravel schedule cronjob is working properly (change config, schedule, pruning expired limit to 1 minute)
file previews
implement all toolbar functions
toast notification for actions
Make "in <folder>" in uploadInfo a jump link and highlight/blink the file after jumping to it
implement sorting categories in the place of toolbar when no files are selected
implement DynamicClip animations for more components/pages
implement server-side validation for files and folders (size, allowed characters)
check for duplication when uploading - add (1) or more numbers to end of file name if there is a duplicate in same path
check for duplication when user tries to add folders, prevent folder creation if there is duplicate in same path
fix date (try get timezone offset? laravel settings/save timezone on database and deal with conversions?)
decrease header sizings?
box shadows?
underline/bold sidebar links if user is on that page
file list sorting based on header row label/arrow icon clicks
shift click for item selection
implement "space left" on bottom left corner
add left click dropdown menu
add tooltips (in gap inside toolbar/space after sorting categories?)
double click files/folders to view
file sharing
settings page
react virtualisation for large lists
gallery
starred
recycle bin
finish implementing search function
malware check
check if file extension column is not needed on database and then remove if so
add backgrounds/more details for loading pages
add info and tips readme file that is stored in all user's file list
add default files/folders for showcasing along with "restore default files" if user's want them back if they deleted them (without resetting user data and tell them that this won't)
responsive CSS and check if correct on other browsers
email verification
allow Google and Facebook to be used for logins? (OAuth)
add functionality for deleting older users when new user created and space is getting low, etc, and then add extra info under login form for invalid details that the invalid username may be a deleted account and the user can register it.
remove useState import from all files where it's not used
change backend env secret file for render.com if changed
add more automated tests
implement Redis?
test all features of entire app
clear laravel storage log