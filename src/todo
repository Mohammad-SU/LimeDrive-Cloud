frontend axios baseURL env (dont gitignore)
automated testing
make tokens expire in a month and make frontend delete token if expired
file uploads
malware check
file previews
folders
file sharing
settings page
responsive css
store username on database as case sensitive, convert username to case insensitve for checks?
email verification
allow google and facebook to be used for logins? (Oauth)
change database info after deployment
modify allowed-origins in cors config if deploying backend separately
remove usestate import from all files where its not used
set env for render.com (database and aws)