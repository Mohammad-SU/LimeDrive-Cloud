import { useState, memo } from 'react'
import "./File.scss"

function File({ id, content, searchContent, title, dateCreated, dateMod }) {
    return (
        <div className="File">

        </div>
    )
}

export default memo(File)
